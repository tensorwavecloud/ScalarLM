# These are infra and sdk dependencies
# Don't put vLLM or Megatron dependencies in here because they will bloat the client
numpy<2.0.0
jsonlines
aiofiles
persist-queue
matplotlib
streaming-form-data==1.15.0
mpi4py==4.0.3
openai>=1.40.0  # Ensure modern openai package (ensure types module present)
openai-harmony
humanize
atomicwrites
cryptography
sortedcontainers
datasets

