# GPU Configuration (NVIDIA)
BASE_NAME=cuda
VLLM_TARGET_DEVICE=cuda
VLLM_BRANCH=rschiavi/vllm-adapter
VLLM_REPO=https://github.com/funston/vllm.git

# GPU-specific settings
DOCKER_PLATFORM=linux/amd64  # Most GPU servers are AMD64