# Configuration for local vLLM development
BASE_NAME=cpu
VLLM_TARGET_DEVICE=cpu

# Use local vLLM directory at ./vllm
VLLM_SOURCE=local

# These are ignored when VLLM_SOURCE=local
VLLM_BRANCH=rschiavi/vllm-adapter
VLLM_REPO=https://github.com/funston/vllm.git

# Platform
DOCKER_PLATFORM=linux/arm64